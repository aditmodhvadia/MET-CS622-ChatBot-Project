package com.fazemeright.library.api.firebase

/*
class OnCompleteForOnTaskAuthResultAdapter(
        private val onTaskCompleteListener: OnTaskCompleteListener<UserAuthResult?>) : OnCompleteListener<AuthResult> {
    override fun onComplete(task: Task<AuthResult>) {
        if (task.isSuccessful) {
            onTaskCompleteListener
                    .onComplete(withData(UserAuthResultAdapterForAuthResult(task.result)))
        } else {
            onTaskCompleteListener
                    .onComplete(Result.exception(task.exception))
        }
    }

}*/
